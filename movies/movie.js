class Movie {
    constructor(id, name, creator_id, image_url, secrets)
    {
        this.id = id
        this.name = name
        this.creator_id = creator_id
        this.image_url = image_url
        this.secrets = secrets
    }
}

module.exports = Movie